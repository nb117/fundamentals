#!/usr/bin/env python3

def print_usage():
    import skilstak.colors as c
    print(c.cl + c.x + """work in progress"""

if __name__ == "__main__":
    import hello

    name, option = hello.parse_args()
    message = "Hello, " + name + "!"

    try:
        






    except KeyboardInterrupt:
        exit(0)
